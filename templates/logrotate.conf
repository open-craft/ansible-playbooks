{% for item in TARSNAPPER_JOBS %}
{{ LOG_DIR }}/{{ item.name }}.log {
        weekly
        rotate 10
        compress
        delaycompress
        missingok
        notifempty
        create 644 root root
}
{% endfor %}
