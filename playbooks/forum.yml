---
- name: Set up the Discourse instances
  hosts: forum
  become: true
  roles:
    - role: common-server
      tags: 'common-server'

    - role: docker-compose
      tags: 'docker'

    - role: discourse
      tags: 'discourse'
