{
    "Directory": "./data/",
    "Profiles":[
        {
            "Name":     "{{ MATTERMOST_LOGIN }}",
            "Channels": ["{{ MATTERMOST_CHANNEL }}"],

            "Email":{
                "ImapServer": "{{ EMAIL_SERVER }}",
                "Username":   "{{ EMAIL_USERNAME }}",
                "Password":   "{{ EMAIL_PASSWORD }}"
            },

            "Mattermost":{
                "Server":   "{{ MATTERMOST_URL }}",
                "Team":     "{{ MATTERMOST_TEAM }}",
                "User":     "{{ MATTERMOST_LOGIN }}",
                "Password": "{{ MATTERMOST_PASSWORD }}"
            }
        }
    ]
}