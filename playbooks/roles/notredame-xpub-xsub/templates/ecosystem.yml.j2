apps:
  - name      : ndxps
    script    : {{ nexus_pubsub_code_dir }}/lib/index.js
    cwd       : {{ nexus_pubsub_code_dir }}
    exec_mode : fork
    env       :
      NODE_ENV  : '{{ nexus_pubsub_env }}'
      XSUB_PORT : '8700'
      XPUB_PORT : '8701'
