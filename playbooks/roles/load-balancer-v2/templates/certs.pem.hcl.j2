template {
  source           = "{{ consul_template_tmpl_dir }}/certs.pem.ctmpl"
  destination      = "/etc/haproxy/certs.pem"
  create_dest_dirs = false
  backup           = true
  command          = "flock -n -E 0 /tmp/haproxy-reload-lock service haproxy reload"
}
