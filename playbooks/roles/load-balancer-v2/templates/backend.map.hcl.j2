template {
  source           = "{{ consul_template_tmpl_dir }}/backend.map.ctmpl"
  destination      = "/etc/haproxy/backend.map"
  create_dest_dirs = false
  backup           = true
  command          = "flock -n -E 0 /tmp/haproxy-reload-lock service haproxy reload"
}
