template {
  source           = "{{ consul_template_tmpl_dir }}/haproxy.cfg.ctmpl"
  destination      = "/etc/haproxy/haproxy.cfg"
  create_dest_dirs = false
  backup           = true
  command          = "flock -n -E 0 /tmp/haproxy-reload-lock service haproxy reload"
}
